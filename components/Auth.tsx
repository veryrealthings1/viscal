import React from 'react';

// This component is no longer used as Firebase Authentication has been disabled.
// It is kept as a placeholder to prevent import errors in case it's still referenced somewhere.
const Auth: React.FC = () => {
  return null;
};

export default Auth;
